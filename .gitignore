# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg
.env
.venv/
venv/

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Data — raw is committed; large binaries are not
data/processed/*
!data/processed/.gitkeep
*.csv
*.bin
*.tdms
*.lvm

# Figures — always regenerated from scripts
docs/**/*.png
docs/**/*.pdf
docs/**/*.svg

# IDE
.vscode/
.idea/
*.suo
.history/

# KiCad session / lock / autosave / backup cruft (not the design files)
*.kicad_prl
*.lck
~*.lck
*-backups/
*.kicad_pcb-bak
_autosave-*
fp-info-cache

# OS
Thumbs.db
.DS_Store

# Claude Code local state (worktrees, job state, machine-local settings)
.claude/

# Upstream reference clone — kept locally only; see context.md for the repo URL
docs/Vernier-Current-Driver/

# KiCad DRC/ERC report exports (regenerated on demand)
*_drc_violations.json
*_erc_violations.json
